name: Build ICU (macOS)

on:
  workflow_dispatch:
  workflow_call:


env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  # pgAdmin supports macOS 14 (Sonoma) and above; see openssl-macos.yml for
  # why this is pinned rather than inherited from the runner's SDK.
  MACOSX_DEPLOYMENT_TARGET: '14.0'
  # Reserve space in the Mach-O headers for longer install names than the
  # ones linked in. Consumers relocate these libraries into their own bundles
  # and rewrite the references with install_name_tool, and an @loader_path
  # path is longer than the /opt/pgbuild one it replaces. Without the padding
  # the rewrite fails outright with "larger updated load commands do not fit",
  # which is what happened to pg_restore in pgAdmin's appbundle build.
  LDFLAGS: '-Wl,-headerpad_max_install_names'
  PGBUILD_PREFIX: /opt/pgbuild

# Declared rather than inherited. A called workflow runs with the CALLER's
# permissions, and a push-triggered run does not get the same default token
# as a scheduled or dispatched one: every push-triggered run of this
# orchestrator failed at Create Release with 403 whilst every scheduled one
# succeeded, and because the Windows leaves delete before they create, each
# failure left no release at all until the next schedule repaired it.
permissions:
  contents: write

jobs:
  get-versions:
    uses: ./.github/workflows/manifest.yml
    with:
      config-path: manifest.json

  build-icu:
    needs: get-versions

    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64

    runs-on: ${{ matrix.runner }}

    env:
      ICU_VERSION: ${{ needs.get-versions.outputs.ICU_VERSION }}

    steps:
      - name: Prepare install prefix
        run: |
          sudo mkdir -p "${PGBUILD_PREFIX}"
          sudo chown -R "$(id -u):$(id -g)" "${PGBUILD_PREFIX}"

      # The tarball rather than the zip the Windows leaf takes from the same
      # release, because it keeps the executable bit on configure and
      # runConfigureICU.
      - name: Download
        run: |
          curl -fL --retry 5 --retry-delay 5 --retry-all-errors --connect-timeout 30 \
              "https://github.com/unicode-org/icu/releases/download/release-${ICU_VERSION}/icu4c-${ICU_VERSION}-sources.tgz" \
              -o "icu4c-${ICU_VERSION}-sources.tgz"
          tar zxf "icu4c-${ICU_VERSION}-sources.tgz"

      # --enable-rpath is what makes these dylibs usable anywhere else. ICU's
      # Darwin rules otherwise set each library's install_name to the bare leaf
      # name, so anything linking against them records a load command with no
      # directory in it and cannot find the library at run time without
      # DYLD_LIBRARY_PATH being set, which System Integrity Protection strips
      # from a protected process anyway. With it, the install name is the
      # absolute path under the prefix, which is what every other package here
      # produces and what consumers rewrite with install_name_tool.
      #
      # The test and sample programs are a large part of the build and nothing
      # here runs them, so they are not built.
      - name: Configure
        run: |
          cd icu/source
          ./runConfigureICU MacOSX \
              --prefix="${PGBUILD_PREFIX}/icu" \
              --enable-rpath \
              --disable-samples \
              --disable-tests

      - name: Build
        run: |
          cd icu/source
          make -j"$(sysctl -n hw.ncpu)"

      - name: Install
        run: |
          cd icu/source
          make install

      - name: Checkout local actions
        uses: actions/checkout@v6
        with:
          path: _src
          sparse-checkout: |
            .github/actions/generate-spdx-posix
            .github/actions/publish-release
          sparse-checkout-cone-mode: false

      - name: Generate SPDX manifest
        uses: ./_src/.github/actions/generate-spdx-posix
        with:
          package: icu
          install-dir: ${{ env.PGBUILD_PREFIX }}/icu

      - name: Package
        run: |
          tar -czf "${RUNNER_TEMP}/icu-macos-${{ matrix.arch }}-latest.tar.gz" \
              -C "${PGBUILD_PREFIX}" icu

      - name: Upload Binaries
        uses: actions/upload-artifact@v7
        with:
          if-no-files-found: error
          name: icu-${{ env.ICU_VERSION }}-macos-${{ matrix.arch }}
          path: ${{ runner.temp }}/icu-macos-${{ matrix.arch }}-latest.tar.gz

      - name: Publish Release
        # Only from the default branch. A build on a working branch is a
        # test of the build, and the rolling tags are shared: publishing
        # from a branch overwrites whatever main last produced, and main's
        # next scheduled run overwrites that in turn, so whichever ran last
        # decides what everybody downloads.
        if: github.ref == 'refs/heads/main'
        uses: ./_src/.github/actions/publish-release
        with:
          asset: ${{ runner.temp }}/icu-macos-${{ matrix.arch }}-latest.tar.gz
          tag: icu-macos-${{ matrix.arch }}-latest
          title: ICU (macOS ${{ matrix.arch }})
          version: ${{ env.ICU_VERSION }}
          immutable-tag: icu-${{ env.ICU_VERSION }}-macos-${{ matrix.arch }}
          immutable-title: ICU ${{ env.ICU_VERSION }} (macOS ${{ matrix.arch }})
